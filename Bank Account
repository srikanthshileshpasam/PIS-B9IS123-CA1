#Create a bank account class
#Define functions - deposit, withdraw, transfer, check balance.
#Create some bank accounts and test your methods.
#Try to prevent illegal actions on account.

class BankAccount:
    def __init__(self, my_acc_num, other_acc_num, amount):
        self.my_acc_num = my_acc_num
        self.other_acc_num = other_acc_num
        self.amount = amount
        
    def deposit(self):
        
        
    def withdraw(self):
        
    def balance(self):
        
    def transfer(self):
            
            
class Authentication:
    def __init__(self, acc_id, pwd):
        self.acc_id = acc_id
        self.pwd = pwd
        
    def check(self):
        
        
        
user_name = input('Enter your user name below:\n')
password = input('Enter your password below:\n')

check_credentials = Authentication()
