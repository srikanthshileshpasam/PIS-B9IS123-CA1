import random

num_lst = []
for i in range(0,9999):
    num_lst.append(random.randint(1,99999999))
#print(sorted(num_lst))

rand_name_dir = ('Nami', 'Yuumi', 'Pyke', 'Soraka')
name_lst = []
for i in range(0,9999):
    name_lst.append(random.choice(rand_name_dir))
#print(name_lst)

